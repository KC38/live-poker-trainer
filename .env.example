# Developer config, not player-facing: the key ships inside the build.
# Required setup: `cp .env.example .env`, then set the key below in `.env`.
# `.env` is a declared Flutter asset (pubspec.yaml) and is never committed.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
