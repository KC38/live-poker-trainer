Run continuous live play → test → fix using the project skill
`.cursor/skills/play-test-fix/SKILL.md`.

Sync `main`, play on agent iPhone 17 Pro only (UDID-pinned), leave the user
iPhone 17 alone, ship product bugs via `/ship-change`, and keep iterating
until the user says stop.

Report each batch: hands, coach verdicts, PRs, HEAD, sync status.
