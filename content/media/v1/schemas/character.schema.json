{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://livepokertrainer.local/schemas/media/v1/character.schema.json",
  "title": "Character",
  "description": "Cast member for learning-platform media. Visual identity must never imply a playing-style archetype.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "displayName",
    "role",
    "silhouetteNotes",
    "palette",
    "teachingPurpose",
    "castingRestrictions"
  ],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^character\\.(coach|player)\\.[a-z0-9]+(\\.[a-z0-9_-]+)*$",
      "description": "Stable character ID, e.g. character.coach.primary."
    },
    "displayName": {
      "type": "string",
      "minLength": 1
    },
    "role": {
      "type": "string",
      "enum": ["coach", "supporting"]
    },
    "silhouetteNotes": {
      "type": "string",
      "minLength": 1,
      "description": "Readable silhouette / pose guidance for artists."
    },
    "palette": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^#([0-9A-Fa-f]{6}|[0-9A-Fa-f]{8})$"
      },
      "description": "Hex colors aligned with AppColors felt-lab direction."
    },
    "teachingPurpose": {
      "type": "string",
      "minLength": 1,
      "description": "Pedagogical job this character serves; not a playing style."
    },
    "castingRestrictions": {
      "type": "string",
      "minLength": 1,
      "description": "Must state that appearance must not imply a playing-style archetype (nit, LAG, etc.) or protected-class stereotypes."
    }
  }
}
