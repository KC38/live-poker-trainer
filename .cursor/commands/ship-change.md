Ship this change using the project skill `.cursor/skills/ship-change/SKILL.md`.

Run the full pipeline: feature branch → implement/verify (if not done) → commit → review → push/PR → squash-merge to main → delete old branch → sync local main → refresh Flutter simulator if available.

Report: branch name, PR URL, merge result, simulator action taken.
